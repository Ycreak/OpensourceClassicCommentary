FROM couchdb:latest

# Expose CouchDB port
EXPOSE 5984

# CouchDB credentials are injected at runtime through the COUCHDB_USER and
# COUCHDB_PASSWORD environment variables (see docker-compose.yml). 

